import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    hmr: {
      host: 'localhost',
      port: 5173, // Ensure this matches the port your Vite server is using
    },
  },
});
